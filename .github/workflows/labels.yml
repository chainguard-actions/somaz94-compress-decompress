name: Sync Labels

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 1'

permissions:
  issues: write

jobs:
  labels:
    uses: somaz94/.github/.github/workflows/labels-sync-reusable.yml@f985f57e4970eba23bdc913f91933813d7cab8fa # main
