name: Lock Threads

on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  lock:
    uses: somaz94/.github/.github/workflows/lock-threads-reusable.yml@4395663967fb07d44defafb52018c667ae383897 # main
