name: Lock Threads

on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  lock:
    uses: somaz94/.github/.github/workflows/lock-threads-reusable.yml@f985f57e4970eba23bdc913f91933813d7cab8fa # main
