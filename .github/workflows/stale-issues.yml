name: Close Stale Issues

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  stale:
    uses: somaz94/.github/.github/workflows/stale-issues-reusable.yml@4395663967fb07d44defafb52018c667ae383897 # main
