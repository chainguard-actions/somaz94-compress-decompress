name: PR Size Labeler

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  pull-requests: write

jobs:
  size:
    uses: somaz94/.github/.github/workflows/pr-size-labeler-reusable.yml@f985f57e4970eba23bdc913f91933813d7cab8fa # main
