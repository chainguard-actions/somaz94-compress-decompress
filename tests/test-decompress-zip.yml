name: Test decompress zip format
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create and zip source files
        run: |
          mkdir -p "$GITHUB_WORKSPACE/decomp-src"
          echo "Hello Decompress" > "$GITHUB_WORKSPACE/decomp-src/hello.txt"
          echo "Second file content" > "$GITHUB_WORKSPACE/decomp-src/second.txt"
          mkdir -p "$GITHUB_WORKSPACE/decomp-archive"
          cd "$GITHUB_WORKSPACE" && zip -r decomp-archive/test.zip decomp-src/

      - id: decompress
        uses: .
        with:
          command: decompress
          source: decomp-archive/test.zip
          dest: decomp-output
          format: zip

      - name: Assert file_path output is set
        env:
          ACTUAL: ${{ steps.decompress.outputs.file_path }}
        run: |
          echo "actual file_path: $ACTUAL"
          if [ -z "$ACTUAL" ]; then
            echo "FAIL: file_path output is empty"
            exit 1
          fi
          echo "PASS: file_path is set to $ACTUAL"

      - name: Assert decompressed files exist
        run: |
          echo "Contents of decomp-output:"
          find "$GITHUB_WORKSPACE/decomp-output" -type f 2>/dev/null || echo "No files found"
          # Check that at least one file was extracted
          FILE_COUNT=$(find "$GITHUB_WORKSPACE/decomp-output" -type f | wc -l)
          echo "File count: $FILE_COUNT"
          if [ "$FILE_COUNT" -lt 1 ]; then
            echo "FAIL: no files were decompressed"
            exit 1
          fi
          echo "PASS: files were decompressed successfully"

      - name: Assert hello.txt content is correct
        run: |
          HELLO_FILE=$(find "$GITHUB_WORKSPACE/decomp-output" -name "hello.txt" | head -1)
          if [ -z "$HELLO_FILE" ]; then
            echo "FAIL: hello.txt not found in decompressed output"
            find "$GITHUB_WORKSPACE/decomp-output" -type f
            exit 1
          fi
          CONTENT=$(cat "$HELLO_FILE")
          echo "Content: $CONTENT"
          if [ "$CONTENT" != "Hello Decompress" ]; then
            echo "FAIL: expected 'Hello Decompress' but got '$CONTENT'"
            exit 1
          fi
          echo "PASS: hello.txt content is correct"
